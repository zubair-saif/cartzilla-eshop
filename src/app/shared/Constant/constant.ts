
export enum SORT {
    Low = "Low",
    High = "High",
    Name = "Name"
}

export enum API_ENDPOINT {
    PRODUCTS = "/products/",
    PRODUCT_CATEGORIES = "/products/categories",
    PRODUCT_CATEGORY = "/products/category/"
}